package com.stockms.domain.model;

/** Physical location types within the store. */
public enum LocationType {
    /** Back-of-store reserve/warehouse area. */
    RESERVE,
    /** Sales floor aisle / shelf. */
    AISLE
}
